<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <!-- A linha 'package="com.example.solveit"' foi REMOVIDA daqui, como recomendado pelo Android Studio -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SolveIt"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".HomeActivity"
            android:exported="false"
            android:theme="@style/Theme.SolveIt.BlueStatusBar" />
        <activity
            android:name=".NotificacoesActivity"
            android:exported="false" />
        <activity
            android:name=".ListaChamadosActivity"
            android:exported="false"
            android:theme="@style/Theme.SolveIt.BlueStatusBar" />
        <activity
            android:name=".AberturaChamadoActivity"
            android:exported="false"
            android:theme="@style/Theme.SolveIt.BlueStatusBar" />
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="false"
            android:theme="@style/Theme.SolveIt.MaterialNoActionBar" />
        <activity
            android:name=".RegisterActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DetalheChamadoActivity"
            android:exported="false"
            android:theme="@style/Theme.SolveIt.BlueStatusBar" />


        <!-- ✨ DECLARAÇÃO DA TELA DE PERFIL ADICIONADA ✨ -->
        <activity android:name=".PerfilActivity" />

    </application>

</manifest>